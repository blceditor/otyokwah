Please create a plan that follows our TDD flow and follows these rules:
1. You only pause in the event of a true blocker. A true blocker is one where you have tried 5 different solutions that aren't hacks yourself before giving up. In addition, you must assume that you are the problem first before saying a dependency is a problem and try fixing the integration 5 times before blocking. If you don't have exact instructions on how to handle a situation use your exec-team to analyze the situation and debate the solutions. If the team finds a consensus solution use it unless you're introducing a major design change or new technology. 
2. Fix all P0, P1, and judgement-based P2s and P3s recursively at least 4 times at each point where a review has identified PX recommendations and issues. 
3. You must commit all changes when you're done and you've run a full set of local tests, lint checks, everything locally.
4. You will monitor the deployment (wait 2 minutes and then check every 30s for another 5 minutes) for success.
5. IF NOT SUCCESS - investigate all the potential issues qplan solutions for them with TDD implemntation and run our normal TDD process on those. Repeating recursively for up to 4 times.
6. You'll validate both via production "html" checks and playwright tests that validate that the feature works on the website pages that render and in the keystatic CMS as an editor (as needed, but if it is a component or a feature that shows in both places it should have both tests.  Plan for these tests earlier so they are built as failing tests and ready to validate once we deploy) You'll use the smoke-test.sh script also to validate the pages are all there and functioning.  ANY AND ALL issues that are found get a qplan and execution of our TDD flow just like this again.  
7. You will validate all UI requirements for the plan using the claude chrom extension.
8. After all of that, double check that you actually completed all of the requirements fully.  Give a report confirming all requirements are 100% complete with full test coverage.  Give me a report showing all the playwright tests succeeding and a link to any screenshots (directory is fine). A report that all the smoke-test.sh tests succeeded. A report showing the full local testing with the number of tests executed (broken down by type) vs passing, the test coverage percentage at each level of testing.
9. ANY AND ALL issues we find, including pre-existing ones, get fixed by us.  We are the only developer working on this so it was our fault and we take ownership to fix.