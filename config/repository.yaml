# Repository & Site Configuration
# SINGLE SOURCE OF TRUTH for all transferable configuration
#
# To change values:
# 1. Edit this file
# 2. Run: npm run generate:config
# 3. Commit the generated files
#
# Generated files:
# - lib/config/repository.ts (GitHub constants)
# - lib/config/site.ts (domain/URL constants)
# - tests/fixtures/config.ts (test fixtures)

# GitHub Repository Configuration
github:
  # Repository owner (GitHub username or organization)
  owner: blceditor
  # Repository name
  repo: otyokwah
  # Full repository path (owner/repo)
  full: blceditor/otyokwah
  # GitHub App slug for Keystatic CMS
  app_slug: otyokwah-keystatic
  # GitHub repository URL
  url: https://github.com/blceditor/otyokwah

# Site/Domain Configuration
site:
  # Production domain (without protocol)
  production_domain: otyokwah.vercel.app
  # Production URL (with protocol)
  production_url: https://otyokwah.vercel.app
  # Keystatic CMS URL
  keystatic_url: https://otyokwah.vercel.app/keystatic

# Contact Email Configuration
email:
  # General contact email
  contact: info@otyokwah.org
  # Camp registrar email
  registrar: info@otyokwah.org
  # Default contact (used in CMS defaults)
  default: info@otyokwah.org

# Analytics Configuration
analytics:
  # Google Analytics 4 measurement ID (TBD - set up later)
  ga4_measurement_id: ""

# Branding Colors
branding:
  colors:
    # Deep forest green
    primary: "#2D5A27"
    # Dark charcoal
    secondary: "#333333"
    # Gold accent
    accent: "#C4A84B"
    # Off-white background
    cream: "#F5F0E8"
    # Sandy tan
    sand: "#D4C5A9"
    # Stone grey
    stone: "#8B7D6B"
    # Dark bark brown
    bark: "#3D2E1F"

# Vercel Configuration (project-specific, not transferable)
# These stay as environment variables - not generated
# vercel:
#   project_id: (from VERCEL_PROJECT_ID env var)
#   org_id: (from VERCEL_ORG_ID env var)
