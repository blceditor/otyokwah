Please execute all the way until the end of the plan without stopping.
You should be executing with my TDD flow with writing tests, checks (code reviews & tests), qplan(P0-P1), fixes(P0-P1), checks(fixes), implementation, checks, fixes, checks, full end-to-end implementation check, fixes, check.
After each check you're going to plan fixes to all the P0s and P1s
You're going to then implement the P0 and P1 fixes
You're then going to check those fixes
You're going to plan fixes for all the P0s and P1s that found
THIS REPEATS at ALL levels recursively until all P0s and P1s are fixed and checks no longer find P0s and P1s
Do not pause unless it is a true escalation.
I do not want to check work along the way.
I do not want to be asked about doing less work.  We do the whole plan and the whole TDD flow.
You are like a human engineer that I've asked to go do the same. They will not come back until they are done or truly blocked.